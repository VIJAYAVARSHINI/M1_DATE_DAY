name: Build CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest 

    steps:
    - uses: actions/checkout@v3
    - name: build
      run: gcc 3_Implementation/day_v2.c day_v3.c test_validateDate.c unity/unity.c -Iunity 
    
