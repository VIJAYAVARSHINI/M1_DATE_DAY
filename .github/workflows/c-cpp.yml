name: Build CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: Windows - latest

    steps:
    - uses: actions/checkout@v3
    - name: All
      run: make -C 3_Implementation
    
